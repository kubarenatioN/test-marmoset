'use client';

import { FC } from 'react';

type PageProps = object;

const page: FC<PageProps> = ({}) => {
  return (
    <div>
      <section>place here</section>

      {/* <MarmosetScene /> */}
    </div>
  );
};

export default page;
